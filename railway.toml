[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile.railway"
watchPatterns = ["/**", "!/report_snapshot.json", "!/public/legacy-report.html"]

[deploy]
startCommand = "python railway_cron.py"
cronSchedule = "30 21 * * 1-5"
restartPolicyType = "NEVER"
